
// This file is for shared TypeScript types.
// For this application, most types are simple and defined within the components.
// However, for a larger app, you might define shared interfaces here.

export interface UploadResult {
  url: string;
  fileName: string;
}
